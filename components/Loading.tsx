import * as React from "react";

interface ILoadingProps {
  text: string;
}

const Loading: React.FunctionComponent<ILoadingProps> = ({ text }) => {
  return <div></div>;
};

export default Loading;
